import type { Vehicle, Customer, Rental, Invoice } from '../types';

// Data jsou nyní načítána z databáze Supabase. Tento soubor již není aktivně používán pro uchovávání stavu.
export const MOCK_VEHICLES: Vehicle[] = [];
export const MOCK_CUSTOMERS: Customer[] = [];
export const MOCK_RENTALS: Rental[] = [];
export const MOCK_INVOICES: Invoice[] = [];
